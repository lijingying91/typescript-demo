export function sayHi (template: any, name: string, age: number) {
	console.log(template);
	console.log(name);
	console.log(age)
}
export function sayHello(name: string) {
    return `Hello from ${name}`;
}